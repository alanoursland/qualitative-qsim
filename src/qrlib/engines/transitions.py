"""QSIM P- and I-transition tables (phase 1 — not yet implemented).

These are data, not code: per-variable tables of admissible
``(qmag, qdir) -> {(qmag', qdir'), ...}`` moves when leaving a time point
(P-transitions) or a time interval (I-transitions), transcribed from
Kuipers (1994) with the book's row labels kept in comments for audit.
See docs/qsim.md §2.
"""
