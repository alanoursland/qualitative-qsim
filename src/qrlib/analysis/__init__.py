"""Analysis over behavior graphs (phases 1 and 7 — not yet implemented).

Planned contents per docs/architecture.md and docs/host-integration.md
(Surface 4): ``queries`` (reachability, quiescent-state and cycle
enumeration, path predicates — built on qrlib.graph, no external graph
deps) and ``explain`` (structured step records + prose narration of
behaviors).
"""
